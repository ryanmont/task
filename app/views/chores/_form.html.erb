<%= form_with(model: chore, local: true) do |form| %>
  <% if chore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chore.errors.count, "error") %> prohibited this chore from being saved:</h2>

      <ul>
      <% chore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :chore_title %>
  </div>

  <div class="field">
    <%= form.label :kid %>
    <%= form.text_field :kid, id: :chore_kid %>
  </div>

  <div class="field">
    <%= form.label :other %>
    <%= form.text_field :other, id: :chore_other %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
